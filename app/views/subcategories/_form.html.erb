<%= form_with(model: subcategory, local: true) do |f| %>
  <% if subcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subcategory.errors.count, "error") %> prohibited this subcategory from being saved:</h2>

      <ul>
      <% subcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="field">
    <%= f.label :category %>
    <%= f.select :category_id, Category.pluck(:name, :id), {}, class: "form-control" %>
  </div>

  <div class="field">
    <%= f.submit %>
  </div>
<% end %>
